#!/bin/bash

# There are a few packages that need to be installed before this package can be run.
# Run this script to install them automatically.

sudo apt-get install ros-noetic-gazebo-ros-pkgs ros-noetic-gazebo-ros-control
sudo apt-get install ros-noetic-move-base
